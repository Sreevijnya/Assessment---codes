class Solution:
    def isValid(self, s: str) -> bool:
        st,m=[],{')':'(',']':'[','}':'{'}
        for c in s: 
            if c in m.values(): st.append(c)
            elif not st or st.pop()!=m[c]: return False
        return not st
            




class Solution:
    def longestValidParentheses(self, s: str) -> int:
        res,stack=0,[-1]
        for i,c in enumerate(s):
            stack.append(i) if c=='(' else stack.pop()
            if stack: res=max(res,i-stack[-1])
            else: stack.append(i)
        return res

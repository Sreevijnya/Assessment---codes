class Solution:
    def containsNearbyDuplicate(self, nums: List[int], k: int) -> bool:
        s=set()
        for i,n in enumerate(nums):
            if n in s:
                return True
            s.add(n)
            if i >= k:
                s.remove(nums[i-k])
        return False
